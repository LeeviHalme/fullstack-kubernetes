name: Build and publish pong-app

on:
  push:
    paths:
      # Only trigger if files in this folder change
      - "pong-app/**"

jobs:
  build-publish:
    name: Build, Push, Release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and publish
        run: |-
          # Build from the specific subdirectory
          docker build -t "leevihalme/pong-app:$GITHUB_SHA" ./pong-app
          docker push "leevihalme/pong-app:$GITHUB_SHA"

      - name: Set up Kustomize
        uses: imranismail/setup-kustomize@v2

      - name: Use right image
        # This part will do nothing as the cluster is running locally,
        # but in a real scenario, we would update the image tag here
        run: |
          cd pong-app
          echo "kustomize edit set image PROJECT/IMAGE=leevihalme/pong-app:$GITHUB_SHA"

      - name: Commit change
        uses: EndBug/add-and-commit@v9
        with:
          add: "pong-app/kustomization.yml"
          message: "New version released: ${{ github.sha }}"
